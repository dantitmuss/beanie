# PartyKit host for multiplayer.
# Local dev (npm run mp:dev):   localhost:1999  (the default if unset)
# Production:                   <project>.<user>.partykit.dev (from npm run mp:deploy)
VITE_PARTYKIT_HOST=localhost:1999
